# RiderPro 完整登录系统 - 最终版本

## 系统特点

✅ **完全独立的登录界面** - 无导航栏，专业美观
✅ **强制登录保护** - 未登录无法访问任何其他页面
✅ **自动重定向** - 智能判断登录状态
✅ **用户会话管理** - 显示用户名和登出功能
✅ **SQLite 数据库** - 本地存储，易于分享

---

## 用户访问流程

```
┌─────────────────────────────────────────────────────┐
│  用户访问任何页面                                      │
│  (http://localhost:8000/*)                           │
└──────────────────┬──────────────────────────────────┘
                   │
                   ▼
         ┌─────────────────┐
         │  检查登录状态    │
         └────────┬─────────┘
                  │
        ┌─────────┴─────────┐
        │                   │
    未登录               已登录
        │                   │
        ▼                   ▼
┌──────────────┐    ┌──────────────┐
│ 跳转到登录页  │    │  显示页面内容 │
│ (index.html) │    │  + 导航栏     │
└──────────────┘    └──────────────┘
        │                   │
        ▼                   ▼
┌──────────────┐    ┌──────────────┐
│ 注册/登录    │    │ 导航栏显示:   │
│              │    │ "Hello, 用户名"│
└──────────────┘    │ + Logout按钮 │
        │            └──────────────┘
        │                   │
        ▼                   ▼
   登录成功         点击 Logout
        │                   │
        │                   ▼
        │            ┌──────────────┐
        └────────────→ 退出并跳转到  │
                     │  登录页      │
                     └──────────────┘
```

---

## 文件结构

```
riderpro-website-main 2/
│
├── index.html              ← 登录/注册页面（独立界面，无导航栏）
├── home.html               ← 主页（需要登录）
├── about.html              ← 关于页面（需要登录）
├── quiz.html               ← 测验页面（需要登录）
├── rankings.html           ← 排行榜（需要登录）
├── demo.html               ← 演示场景（需要登录）
├── Gamification Dashboard.html  ← 游戏化仪表板（需要登录）
├── scenario-training.html  ← 场景训练（需要登录）
├── contact.html            ← 联系页面（旧登录页，已弃用）
│
├── js/
│   ├── auth.js             ← 登录验证中间件（自动保护所有页面）
│   ├── script.js
│   ├── quiz.js
│   ├── rankings.js
│   └── scenario-training.js
│
├── server.js               ← 后端服务器
├── package.json            ← 依赖配置
├── riderpro.db             ← SQLite 数据库文件
│
├── START.bat               ← Windows 一键启动脚本
└── README_DATABASE.md      ← 数据库使用说明
```

---

## 快速启动

### 方式 1: 使用启动脚本（推荐）

**Windows 用户**:
双击 `START.bat` 文件

脚本会自动：
1. 启动后端服务器（端口 3000）
2. 启动前端服务器（端口 8000）
3. 打开浏览器访问登录页

### 方式 2: 手动启动

**步骤 1 - 启动后端**:
```bash
cd "riderpro-website-main 2"
node server.js
```

**步骤 2 - 启动前端**（新终端）:
```bash
cd "riderpro-website-main 2"
python -m http.server 8000
```

**步骤 3 - 访问**:
浏览器打开: http://localhost:8000

---

## 登录界面特点

### 视觉设计
- ✨ **左右分栏布局** - 左侧品牌展示，右侧登录表单
- 🎨 **渐变背景** - 紫色渐变配色，专业美观
- ⭐ **动画效果** - 流畅的加载和切换动画
- 📱 **响应式设计** - 移动端自动适配

### 功能特性
- 🔄 **Tab 切换** - Login/Register 快速切换
- ✅ **实时验证** - 表单字段验证
- 💬 **友好提示** - 成功/错误消息显示
- 🔒 **密码要求** - 最少6个字符

---

## 安全机制

### 1. 前端保护
- `auth.js` 在每个页面加载时自动检查登录状态
- 未登录用户访问任何页面 → 自动跳转到 index.html
- 已登录用户访问 index.html → 自动跳转到 home.html

### 2. 密码安全
- 使用 bcrypt 加密存储
- 密码永远不会以明文形式存储
- 加密强度: 10 rounds

### 3. 会话管理
- 用户信息存储在 localStorage
- 登出时自动清除所有会话数据
- 刷新页面不会丢失登录状态

---

## 测试账号

数据库中已有测试账号（如果你之前注册过）:

| 邮箱 | 密码 |
|------|------|
| test@example.com | password123 |
| alice@riderpro.com | alice123 |

或者注册新账号进行测试。

---

## 常见使用场景

### 场景 1: 首次访问
1. 打开 http://localhost:8000
2. 看到美观的独立登录界面
3. 点击 "Register" 标签注册
4. 登录成功后自动进入主页

### 场景 2: 已登录用户
1. 已登录状态下访问任何页面
2. 导航栏显示 "Hello, [用户名]"
3. 可以自由浏览所有页面
4. 点击 "Logout" 退出

### 场景 3: 未登录尝试访问
1. 未登录时尝试访问 home.html
2. auth.js 自动拦截
3. 立即重定向到登录页
4. 登录后才能访问

---

## 与小组成员共享

### 方案 1: 每人独立运行
1. 分享整个项目文件夹给小组成员
2. 每人在自己电脑上运行 `START.bat` 或手动启动
3. 每人都需要注册自己的账号
4. 每人有独立的数据库

### 方案 2: 共享同一数据库
1. 一个人运行服务器
2. 查找本机IP地址（如 192.168.1.100）
3. 其他成员访问 http://192.168.1.100:8000
4. 所有人共享同一个数据库
5. 需要在同一WiFi网络

---

## API 接口

### 注册
```
POST http://localhost:3000/api/register
Content-Type: application/json

{
  "fullname": "张三",
  "email": "zhangsan@example.com",
  "phone": "13800138000",
  "password": "password123"
}
```

### 登录
```
POST http://localhost:3000/api/login
Content-Type: application/json

{
  "email": "zhangsan@example.com",
  "password": "password123"
}
```

### 查看所有用户（调试）
```
GET http://localhost:3000/api/users
```

---

## 技术栈

- **前端**: HTML5, CSS3, JavaScript (ES6+)
- **后端**: Node.js, Express.js
- **数据库**: SQLite (sql.js)
- **密码加密**: bcrypt
- **会话管理**: localStorage
- **跨域支持**: CORS

---

## 问题排查

### Q: 访问页面时一直重定向到登录页？
**A**: 这是正常的！未登录状态下所有页面都会重定向。先注册并登录即可。

### Q: 登录后看不到用户名？
**A**: 确保 auth.js 已正确加载。检查浏览器控制台是否有错误。

### Q: 后端报错 "Cannot find module"？
**A**: 运行 `npm install` 安装所有依赖。

### Q: 前端无法连接后端？
**A**: 确认后端服务器运行在 http://localhost:3000

---

## 下一步改进建议

1. ✨ 添加"记住我"功能实现
2. 📧 实现邮箱验证
3. 🔐 添加密码重置功能
4. 👤 用户个人资料页面
5. 🎨 自定义主题选择
6. 📊 用户活动日志

---

## 联系支持

如有问题，请查阅:
- README_DATABASE.md - 数据库详细说明
- 浏览器开发者工具控制台 - 查看错误信息
- 后端终端输出 - 查看API调用日志

---

**版本**: 2.0 Final
**更新日期**: 2025-12-02
**状态**: ✅ 生产就绪
